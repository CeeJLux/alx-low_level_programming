#include <stdio.h>
#include <stdlib.h>

/**
 * main - prints program namr with new line
 * @argc: Argument Counter
 * @arcv: array of pointers supplied 
 *
 * Return: Always 0.
 */
int main(int __attribute((__unused__)) argc, char *argv[])
{
	printf("%s\n", argv[0]);


	return (0);
}
